[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
  "python manage.py migrate --noinput",
  "python manage.py seed_data"
]

[start]
cmd = "gunicorn donation_tracker.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file -"
